# ─── Portfolio Configuration ─────────────────────────────────
# Copy this file to .env.local and fill in your values.
# NEXT_PUBLIC_* vars are exposed to the browser—don't put secrets here.

# Identity
NEXT_PUBLIC_SITE_NAME="Your Name"
NEXT_PUBLIC_SITE_NAME_SHORT="Your.Name"
NEXT_PUBLIC_TAGLINE="Web Developer"
NEXT_PUBLIC_SITE_TITLE="Your Name — Web Developer"
NEXT_PUBLIC_SITE_DESCRIPTION="Full-stack web developer building modern websites for small businesses."
NEXT_PUBLIC_COPYRIGHT="Your Name"

# URL
NEXT_PUBLIC_SITE_URL="https://yourdomain.com"

# Environment (set to "test" for test mode badge, omit for production)
# NEXT_PUBLIC_APP_ENV="test"

# Contact
NEXT_PUBLIC_EMAIL="your@email.com"
NEXT_PUBLIC_CALENDLY="your-calendly-username"
NEXT_PUBLIC_WEB3FORMS_KEY=""

# Google Search Console (optional)
NEXT_PUBLIC_GOOGLE_VERIFICATION=""

# Discord Presence (optional)
NEXT_PUBLIC_DISCORD_ID=""
NEXT_PUBLIC_DISCORD_USERNAME=""

# Socials
NEXT_PUBLIC_GITHUB_URL=""
NEXT_PUBLIC_INSTAGRAM_URL=""

# Location / Pricing
NEXT_PUBLIC_LOCATION="Your City, State"
NEXT_PUBLIC_PRICE_RANGE="$500 - $5,000"

# Share
NEXT_PUBLIC_SHARE_TEXT="Check out this developer 👋"

# ─── Secrets (NOT prefixed with NEXT_PUBLIC_) ────────────────
# On Vercel, add these in your project dashboard under Settings → Environment Variables.
# They are never exposed to the browser.

# Database (Neon Postgres)
DATABASE_URL="postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require"

# Auth
AUTH_SECRET="generate with: openssl rand -base64 32"
AUTH_ADMIN_EMAIL="admin@yourdomain.com"
AUTH_ADMIN_PASSWORD="your-admin-password"

# Stripe
# ──────────────────────────────────────────────────────────────
# For LOCAL TESTING → use Test mode keys (toggle in Stripe Dashboard)
# For PRODUCTION (Vercel) → use Live mode keys
#
# Get keys: https://dashboard.stripe.com/test/apikeys  (test) or /apikeys (live)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."      # or pk_live_...
STRIPE_SECRET_KEY="sk_test_..."                       # or sk_live_...
STRIPE_WEBHOOK_SECRET="whsec_..."                     # from Stripe Dashboard → Webhooks (or from `stripe listen`)

# Email — Resend (recommended for receipts, welcome emails, forum & support notifications)
# Get your key: https://resend.com/api-keys
# For production, verify your domain in Resend and update EMAIL_FROM
RESEND_API_KEY=""
EMAIL_FROM="Hayden Ford <no-reply@haydenf.fyi>"

# Create products & prices in Stripe Dashboard (do this in BOTH Test and Live modes)
# Then paste the correct price_ IDs for the environment you're using:
STRIPE_ESSENTIAL_PRICE_ID="price_..."                 # one-time website package
STRIPE_GROWTH_PRICE_ID="price_..."
STRIPE_PREMIUM_PRICE_ID="price_..."
STRIPE_MAINTENANCE_PRICE_ID="price_..."               # recurring monthly addon (subscription)
